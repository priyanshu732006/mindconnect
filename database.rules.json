
{
  "rules": {
    /* 
      Security Rules Version: 1.0.5
      Last updated: 2024-05-22
      Description: Explicit read rules for peer buddy directory and chats to prevent race conditions.
    */
    "peerBuddies": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "userRoles": {
      ".read": "auth != null",
      ".indexOn": "role",
      "$uid": {
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "studentData": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "chats": {
      "$chatId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
