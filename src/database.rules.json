
{
  "rules": {
    /* 
      Security Rules Version: 1.2.0
      Last updated: 2024-05-24 19:00:00
      Fix: Explicitly allow authenticated root read for peer directory and chats to prevent permission_denied.
    */
    "peerBuddies": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "userRoles": {
      ".read": "auth != null",
      ".indexOn": "role",
      "$uid": {
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "studentData": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "chats": {
      ".read": "auth != null",
      "$chatId": {
        ".write": "auth != null && $chatId.contains(auth.uid)"
      }
    }
  }
}
