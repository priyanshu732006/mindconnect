
{
  "rules": {
    /* 
      Security Rules Version: 1.0.8
      Last updated: 2024-05-22 16:00:00
      Description: Explicitly requiring authentication for peerBuddy directory and chat paths.
    */
    "peerBuddies": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "userRoles": {
      ".read": "auth != null",
      ".indexOn": "role",
      "$uid": {
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "studentData": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "chats": {
      "$chatId": {
        ".read": "auth != null && ($chatId.contains(auth.uid))",
        ".write": "auth != null && ($chatId.contains(auth.uid))"
      }
    }
  }
}
